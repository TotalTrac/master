SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [acs].[NixieRecordDeleteByID]
	@ID	int
AS
BEGIN
	SET NOCOUNT ON;

    DELETE
		[acs].[NixieRecords]
	WHERE
		RECORD_ID = @ID;

	IF @@ROWCOUNT > 0
		RETURN 0;
	ELSE
		RETURN 1;
END
GO