SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO


CREATE PROCEDURE [dbo].[CountryGetByID]
	@ID	int
AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT 
		COUNTRY_ID
		, COUNTRY_CODE2
		, COUNTRY_CODE3
		, COUNTRY_NAME 
	FROM 
		[dbo].[Countries] 
	WHERE 
		COUNTRY_ID = @ID;
END
GO