SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[LocaleGetByID]
	@ID int
AS
BEGIN
	SET NOCOUNT ON;

	SELECT 
		LOCALE_ID
		, LOCALE_KEY
		, LOCALE_TYPEID
	FROM
		[dbo].[Locales]
	WHERE
		LOCALE_ID = @ID
END
GO