SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[LocaleGetByKey]
	@Key char(6)
AS
BEGIN
	SET NOCOUNT ON;

	SELECT 
		LOCALE_ID
		, LOCALE_KEY
		, LOCALE_TYPEID
	FROM
		[dbo].[Locales]
	WHERE
		LOCALE_KEY = @Key
END
GO