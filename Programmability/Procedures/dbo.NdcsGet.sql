SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[NdcsGet]
	
AS
BEGIN
	SET NOCOUNT ON;

    SELECT
		NDC_ID
		, NDC_LATITUDE
		, NDC_LONGITUDE
		, NDC_NAME
		, NDC_STATE
		, NDC_ZIPCODE
    FROM
		[dbo].[NDCs]
	ORDER BY
		NDC_ID;
END
GO