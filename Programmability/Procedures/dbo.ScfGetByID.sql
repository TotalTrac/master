SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[ScfGetByID]
	@ID int
AS
BEGIN
	SET NOCOUNT ON;

    SELECT
		SCF_ID
		, SCF_LATITUDE
		, SCF_LONGITUDE
		, SCF_NAME
		, SCF_NDCID
		, SCF_STATE
		, SCF_ZIPCODE
    FROM
		[dbo].[SCFs]
	WHERE
		SCF_ID = @ID;
END
GO