SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[StampTypesGet]
	
AS
BEGIN
	
	SET NOCOUNT ON;
	
	SELECT
		STAMPTYPE_ID
		, STAMPTYPE_NAME
		, STAMPTYPE_ROWVERSION
	FROM 
		[dbo].[StampTypes]
	ORDER BY
		STAMPTYPE_ID;
END
GO