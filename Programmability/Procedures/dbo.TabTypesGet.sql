SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[TabTypesGet]
AS
BEGIN

	SET NOCOUNT ON;

    SELECT 
		TABTYPE_ID
		, TABTYPE_NAME
		, TABTYPE_ROWVERSION
	FROM 
		[dbo].[TabTypes]
	ORDER BY
		TABTYPE_ID;
END
GO