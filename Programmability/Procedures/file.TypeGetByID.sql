SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [file].[TypeGetByID] 
	@ID int
AS
BEGIN
	SET NOCOUNT ON;

	SELECT
		[TYPE_ID]
		, [TYPE_NAME]
		, TYPE_ROWVERSION		
	FROM 
		[file].[Types]
	WHERE 
		[TYPE_ID] = @ID;
END
GO