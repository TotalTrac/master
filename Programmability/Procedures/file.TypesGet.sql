SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [file].[TypesGet] 
	
AS
BEGIN

	SET NOCOUNT ON;

	SELECT
		[TYPE_ID]
		, [TYPE_NAME]
		, TYPE_ROWVERSION		
	FROM 
		[file].[Types]
	ORDER BY 
		[TYPE_ID];
END
GO