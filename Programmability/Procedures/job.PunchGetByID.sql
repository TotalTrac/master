SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [job].[PunchGetByID]	
	@ID	int	
AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT
		ID		
		, JOBID
		, MACHINENAME
		, STARTTIME
		, STOPTIME
		, USERNAME
		, [ROWVERSION]
	FROM
		[job].[Punches]
	WHERE
		ID = @ID;		
END
GO