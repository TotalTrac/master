SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [job].[QuotesGetAll]

AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT
		ID
		, CLIENTID
		, CREATED
		, CREATEDBYID
		, NAME
		, UPDATED
		, [ROWVERSION]
	FROM
		[job].[Quotes]

END
GO