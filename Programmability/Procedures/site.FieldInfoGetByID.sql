SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO
CREATE PROCEDURE [site].[FieldInfoGetByID]
	@ID	int
AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT
		INFO_ID
		, INFO_TEXT
		, INFO_TITLE
	FROM
		[site].[FieldInfo]
	WHERE
		INFO_ID = @ID;
END
GO