SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO

CREATE PROCEDURE [web].[EventListenerGetByID]
	@ID	int
AS
BEGIN
	SET NOCOUNT ON;

    SELECT 
		EVENTLISTENER_ID
		, EVENTLISTENER_EVENTS
		, EVENTLISTENER_PAGEID
		, EVENTLISTENER_USERID
		, EVENTLISTENER_ROWVERSION
    FROM
		[web].[EventListeners]
	WHERE
		EVENTLISTENER_ID = @ID;
END
GO